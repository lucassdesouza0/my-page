import React from "react";

// import { Container } from './styles';

const Blog: React.FC = () => {
  return (
    <section id="bio" className="bio-container">
      <h2 className="bio-header">BLOG</h2>
      <p>Em construção...</p>
    </section>
  );
};

export default Blog;
